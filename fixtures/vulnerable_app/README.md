# Intentionally vulnerable dry-run fixture

`app.py` deliberately contains a path-traversal flaw and the marker consumed by the deterministic bridge dry-run. Use it only for local authorized validation. It is not a deployable application.
